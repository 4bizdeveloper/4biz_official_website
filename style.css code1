/* --- ADVANCED UI SYSTEM --- */
:root {
    --bg: #050505;
    --accent: #7000FF;
    --secondary: #00F0FF;
    --text: #ffffff;
    --text-dim: #a0a0a0;
    --glass: rgba(255, 255, 255, 0.03);
    --border: rgba(255, 255, 255, 0.1);
    --container-width: 1200px;
    --transition: 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

* { margin:0; padding:0; box-sizing:border-box; }

body { 
    background: var(--bg); 
    color: var(--text); 
    font-family: 'Inter', sans-serif; 
    line-height: 1.6; 
    overflow-x: hidden; 
}

/* Perfect Alignment Container */
.container { 
    max-width: var(--container-width); 
    margin: 0 auto; 
    padding: 0 40px; /* Essential for text alignment */
}

/* --- Particles --- */
#particleCanvas { 
    position: fixed; 
    top: 0; left: 0; 
    width: 100%; height: 100%; 
    z-index: -1; 
    pointer-events: none; 
}

/* --- Ultra-Advanced Navigation --- */
.header { 
    position: fixed; 
    top: 20px; left: 50%; 
    transform: translateX(-50%); 
    width: 90%; 
    max-width: var(--container-width); 
    background: rgba(10,10,10,0.4); 
    backdrop-filter: blur(20px); 
    border: 1px solid var(--border); 
    border-radius: 100px; 
    padding: 12px 30px; 
    z-index: 1000; 
    transition: var(--transition); 
}

.header.scrolled { 
    top: 10px; 
    width: 95%; 
    background: rgba(0,0,0,0.85); 
}

.nav-container { display: flex; justify-content: space-between; align-items: center; }

.nav-links { display: flex; gap: 30px; list-style: none; }

.nav-links a { 
    color: var(--text-dim); 
    text-decoration: none; 
    font-size: 0.9rem; 
    font-weight: 500; 
    transition: 0.3s; 
}

.nav-links a:hover { color: #fff; text-shadow: 0 0 10px var(--accent); }

/* --- High-End Typography --- */
.gradient-text { 
    background: linear-gradient(to right, #fff, var(--accent), var(--secondary)); 
    -webkit-background-clip: text; 
    -webkit-text-fill-color: transparent; 
}

.ultra-heading { 
    font-family: 'Space Grotesk', sans-serif; 
    font-size: clamp(2.5rem, 5vw, 4.5rem); 
    line-height: 1.1; 
    margin-bottom: 25px; 
}

.section-tag { 
    color: var(--accent); 
    font-weight: 700; 
    letter-spacing: 4px; 
    text-transform: uppercase; 
    font-size: 0.75rem; 
    display: block; 
    margin-bottom: 15px; 
}

/* --- Layout Sections --- */
.section-padding { padding: 120px 0; }

.hero { 
    height: 100vh; 
    display: flex; 
    align-items: center; 
    justify-content: center; 
    text-align: center; 
}

.hero-content { max-width: 900px; }

.hero-content p { 
    font-size: 1.2rem; 
    color: var(--text-dim); 
    margin-bottom: 40px; 
    max-width: 650px; 
    margin-left: auto; 
    margin-right: auto; 
}

/* Bento Grid Alignment */
.bento-grid { 
    display: grid; 
    grid-template-columns: repeat(3, 1fr); 
    gap: 25px; 
    margin-top: 60px; 
}

.bento-card { 
    background: var(--glass); 
    border: 1px solid var(--border); 
    padding: 50px 40px; 
    border-radius: 35px; 
    transition: var(--transition); 
    text-align: left; /* Aligns text within cards */
}

.bento-card:hover { 
    border-color: var(--accent); 
    transform: translateY(-10px); 
    background: rgba(112,0,255,0.05); 
}

.icon-wrap { font-size: 2.5rem; color: var(--accent); margin-bottom: 25px; }

/* Split Grid for About */
.reveal-grid {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 80px;
    align-items: center;
}

/* Animations */
.fade-up { opacity: 0; transform: translateY(40px); transition: 1s cubic-bezier(0.2, 0.8, 0.2, 1); }
.fade-up.visible { opacity: 1; transform: translateY(0); }


